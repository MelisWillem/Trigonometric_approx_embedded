#include "globals.h"

/*
 * calculate the sine for any x [-inf,+inf]
 *  accuracy = 3.2 digits
 */
real_t sin_32(real_t x);
/* 
 * calculate the sine for any x [-inf,+inf]
 *  accuracy = 3.2 digits
 */
real_t cos_32(real_t x);

/*
 * calculate the cosine for any x [-inf,+inf]
 *  accuracy = 5.2 digits
 */
real_t sin_52(real_t x);
/* 
 * calculate the sine for any x [-inf,+inf]
 *  accuracy = 5.2 digits
 */
real_t cos_52(real_t x);

/*
 * calculate the cosine for any x [-inf,+inf]
 *  accuracy = 7.3 digits
 */
real_t cos_73(real_t x);
/* 
 * calculate the sine for any x [-inf,+inf]
 *  accuracy = 7.3 digits
 */
real_t sine_73(real_t x);

/*
 * calculate the cosine for any x [-inf,+inf]
 *  accuracy = 12.1 digits
 */
real_t cos_121(real_t x);
/* 
 * calculate the sine for any x [-inf,+inf]
 *  accuracy = 12.1 digits
 */
real_t sin_121(real_t x);




